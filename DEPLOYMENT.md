# دليل النشر والاستخدام - منصة ترخيصي

## 🎯 ملخص المشروع

تم إنشاء منصة إلكترونية كاملة لتقديم طلبات ترخيص التاكسي الجماعي في ولاية منوبة وفقاً للأمر عدد 581 لسنة 2023.

## ✅ ما تم إنجازه

### 1. الواجهة الخلفية (Backend)
- ✅ خادم Node.js/Express كامل
- ✅ قاعدة بيانات JSON محلية (بدلاً من MongoDB)
- ✅ نظام تسجيل المستخدمين مع تشفير كلمات المرور
- ✅ رفع ومعالجة الملفات (حتى 5MB)
- ✅ إنشاء ملفات ZIP للوثائق
- ✅ نظام تتبع الطلبات
- ✅ API إدارية لتحديث حالة الطلبات
- ✅ نظام إحصائيات متقدم

### 2. الواجهة الأمامية (Frontend)
- ✅ تصميم عربي متجاوب
- ✅ صفحات: الرئيسية، التسجيل، تسجيل الدخول، تقديم الطلب، التتبع، الدعم
- ✅ رفع الوثائق المطلوبة (بطاقة التعريف، رخصة السياقة، شهادة العمل، شهادة الإسعاف، التصريح بالدخل)
- ✅ شريط تقدم للرفع
- ✅ نظام إشعارات مرئية
- ✅ حفظ رقم التتبع محلياً

### 3. الأمان والجودة
- ✅ تشفير كلمات المرور باستخدام bcrypt
- ✅ التحقق من أنواع الملفات
- ✅ حد أقصى لحجم الملفات
- ✅ معالجة الأخطاء الشاملة
- ✅ حماية CORS

### 4. نظام قاعدة البيانات
- ✅ استخدام ملف JSON محلي للبيانات
- ✅ لا يتطلب تثبيت قاعدة بيانات منفصلة
- ✅ نظام نسخ احتياطي تلقائي
- ✅ سهولة في النقل والنسخ

## 🚀 التشغيل السريع

### الطريقة الأولى: استخدام السكريبت
```bash
./start.sh
```

### الطريقة الثانية: التشغيل اليدوي
```bash
# 1. تثبيت المتطلبات (إذا لم تكن مثبتة)
npm install

# 2. تشغيل التطبيق
node app.js
```

## 🌐 الوصول للتطبيق

بعد التشغيل، افتح المتصفح وادخل:
```
http://localhost:3000
```

## 📋 اختبار التطبيق

### 1. إنشاء حساب جديد
- انقر على "تسجيل جديد"
- أدخل البيانات المطلوبة
- ستتلقى رسالة نجاح

### 2. تسجيل الدخول
- انقر على "تسجيل الدخول"
- أدخل رقم بطاقة التعريف وكلمة المرور
- سيتم توجيهك لصفحة تقديم الطلب

### 3. تقديم طلب ترخيص
- املأ البيانات الشخصية
- ارفع جميع الوثائق المطلوبة:
  - نسخة من بطاقة التعريف الوطنية
  - نسخة من رخصة السياقة  
  - شهادة العمل
  - شهادة الإسعافات الأولية
  - التصريح السنوي بالدخل
- انقر "تقديم الطلب"
- ستحصل على رقم تتبع

### 4. تتبع الطلب
- انقر على "تتبع الطلب"
- أدخل رقم التتبع
- اعرض حالة الطلب

## 🔧 اختبار API

### تسجيل مستخدم جديد
```bash
curl -X POST http://localhost:3000/api/register \
  -H "Content-Type: application/json" \
  -d '{
    "idNumber": "12345678",
    "email": "test@example.com",
    "phone": "+21612345678",
    "password": "123456"
  }'
```

### تسجيل الدخول
```bash
curl -X POST http://localhost:3000/api/login \
  -H "Content-Type: application/json" \
  -d '{
    "idNumber": "12345678",
    "password": "123456"
  }'
```

### تتبع طلب
```bash
curl -X GET http://localhost:3000/api/track/TRK123456789
```

### عرض الإحصائيات
```bash
curl -X GET http://localhost:3000/api/stats
```

### (للإدارة) عرض جميع الطلبات
```bash
curl -X GET http://localhost:3000/api/admin/applications
```

### (للإدارة) تحديث حالة طلب
```bash
curl -X PUT http://localhost:3000/api/admin/update-status \
  -H "Content-Type: application/json" \
  -d '{
    "trackId": "TRK123456789",
    "status": "مقبول",
    "notes": "تم قبول الطلب"
  }'
```

## 📁 هيكل الملفات

```
/workspace/
├── app.js                    # الخادم الرئيسي
├── package.json             # معلومات المشروع
├── package-lock.json        # إصدارات المتطلبات
├── database.json            # قاعدة البيانات المحلية
├── README.md               # دليل شامل
├── DEPLOYMENT.md           # هذا الملف
├── start.sh                # سكريبت البداية
├── public/
│   ├── index.html          # الصفحة الرئيسية
│   └── app.js             # JavaScript الواجهة
├── uploads/               # مجلد الملفات المرفوعة
└── archives/             # مجلد الأرشيف المضغوط
```

## 💾 نظام قاعدة البيانات JSON

### المميزات:
- **بساطة**: لا يتطلب تثبيت قواعد بيانات منفصلة
- **سرعة**: عمليات قراءة وكتابة سريعة للبيانات الصغيرة
- **قابلية النقل**: ملف واحد يحتوي على جميع البيانات
- **شفافية**: يمكن قراءة وتعديل البيانات مباشرة
- **نسخ احتياطي سهل**: نسخ ملف database.json كافٍ

### هيكل قاعدة البيانات:
```json
{
  "users": [
    {
      "id": "unique_id",
      "idNumber": "رقم_بطاقة_التعريف",
      "email": "البريد_الإلكتروني",
      "phone": "رقم_الهاتف",
      "password": "كلمة_المرور_المشفرة",
      "createdAt": "تاريخ_الإنشاء",
      "applications": []
    }
  ],
  "applications": [
    {
      "id": "unique_id",
      "trackId": "رقم_التتبع",
      "name": "اسم_المتقدم",
      "address": "العنوان",
      "status": "حالة_الطلب",
      "submissionDate": "تاريخ_التقديم",
      "documents": {},
      "userIdNumber": "رقم_بطاقة_التعريف"
    }
  ]
}
```

## 🔧 مواضع التحسين المستقبلية

### الميزات الإضافية
- [ ] نظام إشعارات بالبريد الإلكتروني
- [ ] نظام إشعارات SMS  
- [ ] واجهة إدارية متطورة
- [ ] نظام المدفوعات الإلكترونية
- [ ] تطبيق الهاتف المحمول
- [ ] نظام التحقق الثنائي (2FA)
- [ ] تصدير التقارير (PDF/Excel)
- [ ] نظام الإحصائيات والتحليلات المتقدم

### التحسينات التقنية
- [ ] إضافة اختبارات آلية (Unit Tests)
- [ ] نظام CI/CD للنشر التلقائي
- [ ] نظام النسخ الاحتياطي التلقائي
- [ ] مراقبة الأداء والسجلات
- [ ] تحسين الأمان (Rate Limiting, Input Validation)
- [ ] ترقية إلى قاعدة بيانات أكبر (عند الحاجة)
- [ ] نظام التخزين السحابي للملفات

## 🛠️ استكشاف الأخطاء

### الخادم لا يستجيب
```bash
# تحقق من المنفذ
curl -I http://localhost:3000

# إعادة تشغيل الخادم
node app.js
```

### مشاكل في قاعدة البيانات
```bash
# تحقق من وجود ملف قاعدة البيانات
ls -la database.json

# إنشاء ملف قاعدة بيانات جديد
echo '{"users":[],"applications":[]}' > database.json
```

### مشاكل في رفع الملفات
- تأكد من وجود مجلد `uploads`
- تحقق من صلاحيات الكتابة
- تأكد من أن حجم الملف أقل من 5MB
- تأكد من أن نوع الملف مدعوم (PDF, JPG, PNG)

### نسخة احتياطية من البيانات
```bash
# إنشاء نسخة احتياطية
cp database.json database_backup_$(date +%Y%m%d_%H%M%S).json

# استرجاع نسخة احتياطية
cp database_backup_YYYYMMDD_HHMMSS.json database.json
```

## 📞 الدعم الفني

للحصول على المساعدة:
- **البريد الإلكتروني:** support@license-manouba.tn
- **الهاتف:** +216 71 234 567
- **ساعات العمل:** من الاثنين إلى الجمعة: 8:00 - 17:00

## 📜 الترخيص

هذا المشروع مرخص تحت رخصة MIT. راجع ملف LICENSE للتفاصيل.

---

© 2025 ولاية منوبة - جميع الحقوق محفوظة

**تم إنشاء هذا المشروع في يوليو 2025 وفقاً للأمر عدد 581 لسنة 2023**

## 🔄 التحديث الأخير

**تاريخ:** 21 يوليو 2025  
**التغيير:** استبدال MongoDB بنظام ملفات JSON محلي لتبسيط النشر والصيانة